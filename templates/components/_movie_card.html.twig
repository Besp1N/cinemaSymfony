<a href="{{ path("app_home_movie", {"movie": movie.id}) }}" class="movie-card movie-card-loaded">
    <img loading="lazy" src="{{ asset(movie.image) }}">
    <div class="movie-card-title">{{ movie.title }}</div>
</a>